Doygen generated html output can be found by opening the following file in a browser
	docs/html/index.html

Dependencies:
    - bazel
        sudo apt-get install openjdk-8-jdk
        echo "deb [arch=amd64] http://storage.googleapis.com/bazel-apt stable jdk1.8" | sudo tee /etc/apt/sources.list.d/bazel.list
        curl https://bazel.build/bazel-release.pub.gpg | sudo apt-key add -
        sudo apt-get update && sudo apt-get install bazel
        sudo apt-get install --only-upgrade bazel

        Make sure the version of bazel is atleast 0.21.0 or greater (run command: bazel version)

Pre-installation steps:
    - create a directory /bazel/grpc_server
    - create a directory /bazel/grpc_client
    - give read write permissions to both directories

Building PLC Emulator:
    >> bazel build :pc_emulator_lib
    >> bazel --output_base=/bazel/grpc_server build //src/pc_emulator/grpc_ext:grpc_ext




Running Examples:
-----------------
    Idle-PLC
    --------
	1. Starting PLC
		>> bazel run :example_idle_plc
	2. Starting Communication Module (New terminal)
		>> bazel run :example_comm_module
    Simple-PLC
    ----------
	1. Starting PLC
    		>> bazel run :example_simple_plc
	2. Starting GRPC Server (New terminal):
    		>> bazel --output_base=/bazel/grpc_server run //src/pc_emulator/grpc_ext:grpc_ext -- /home/moses/OpenSCADA/examples/simple_plc
	3. Starting GRPC Client (New terminal)
		>> bazel --output_base=/bazel/grpc_client run :example_grpc_client

    VT-PLC
    ------
        1. Starting PLC
    		>> bazel run :example_vt_plc
	2. Starting GRPC Server (New terminal):
    		>> bazel --output_base=/bazel/grpc_server run //src/pc_emulator/grpc_ext:grpc_ext -- /home/moses/OpenSCADA/examples/vt_plc
	


